# EOD Momentum Scanner — NASDAQ

Сканер акций NASDAQ: **сигнал в 15:30 ET (за 30 минут до закрытия) по неполной свече → вход по цене закрытия → цель +2…+5% за 24–48 часов** (включая положительный гэп следующего дня).

> ⚠️ Никакой алгоритм не может *гарантировать* рост акции. Система оценивает **вероятность** движения на исторических данных
> (бэктест вне выборки: ~49% попаданий против ~34% у среднего по рынку; сигнал в 15:30 сохраняет ~98% качества сигнала по закрытию).
> Это исследовательский инструмент, а не индивидуальная инвестиционная рекомендация. Стоп −3% и позиция 1–2% капитала обязательны.

## Как это работает

1. **15:30 ET** — сканер строит неполную свечу текущего дня из 30-минутных баров (до 15:30) и считает скор по 60 ликвидным акциям.
2. **15:55–16:00 ET** — вход по цене закрытия.
3. **24–48 часов** — выход на целях +2%/+5% (или стоп −3%).
4. Статистика гэпа следующего дня (вне выборки) показывается по каждому тикеру.

## Автоматизация (GitHub Actions + Pages)

- Workflow `.github/workflows/daily_scan.yml` запускается **автоматически в будни в 15:30 ET** и по кнопке (**Actions → Run workflow**).
- Данные кэшируются на раннере (месячный кэш) — ежедневный прогон ~3–5 минут.
- Результаты коммитятся в репозиторий, дашборд публикуется на **GitHub Pages**:
  `https://<логин>.github.io/nasdaq-eod-momentum-scanner/`
- Для Pages нужна одноразовая настройка: **Settings → Pages → Source: GitHub Actions**.

## Что внутри

| Файл | Назначение |
|---|---|
| `output/index.html` | Дашборд (корень Pages) — фавориты, уровни, сценарии, графики, метрики |
| `output/dashboard.html` | Тот же дашборд для просмотра в репозитории |
| `output/scan_results.csv` | Полный рейтинг 60 кандидатов последнего скана |
| `.github/workflows/daily_scan.yml` | Автозапуск в 15:30 ET + ручная кнопка + деплой Pages |
| `src/scan.py` | Скан в 15:30 ET: неполная свеча + скор + статистика гэпов |
| `src/features.py` | 24 признака (моментум, свечи, RSI, тренд, волатильность, объём) |
| `src/backtest.py` | Walk-forward бэктест (обучение на прошлом → тест на будущем) |
| `src/build_dashboard.py` | Автономная сборка дашборда |
| `src/update_universe.py` | Состав NASDAQ (Nasdaq.com screener) |
| `src/download_history.py` | История OHLCV (полная + инкрементальная) |
| `src/prep_panel.py` | Панель признаков для модели |
| `deploy/push.sh` | Публикация обновлений через SSH-деплой-ключ |

## Локальный запуск

```bash
pip install -r requirements.txt
python src/update_universe.py       # состав NASDAQ (однократно/ежедневно)
python src/download_history.py      # история 2 года (первый раз ~10 мин)
INCREMENTAL=1 python src/download_history.py   # далее — быстрое обновление
python src/backtest.py              # бэктест (генерирует панель + OOS)
python src/scan.py                  # скан (сигнал 15:30 ET)
python src/build_dashboard.py       # дашборд
./deploy/push.sh "сигнал 04.09"     # залить на GitHub (SSH-ключ)
```

## Результаты бэктеста (окт 2025 – авг 2026, вне выборки)

- Попадание «рост ≥2% за 48ч»: **48,8%** (база рынка 34,4%)
- Среднее лучшее движение за 48ч: **+2,9%** (база +1,4%)
- Средний результат 2-го дня: **+0,9%** (база +0,3%)
- Стратегия опережала базу во **все 11 месяцев**

## Валидация сигнала в 15:30 (неполная свеча)

125 торговых дней × 60 ликвидных акций:
- Корреляция скоров 15:30 vs закрытие: **0,98**
- Пересечение топ-10: **8,4 из 10**
- Хит-рейт ≥2% по сигналу 15:30: **54%** (по закрытию 56%) — потеря всего ~2 п.п.

## О гэпе следующего дня

Статистика вне выборки: после сигналов модели вероятность положительного гэпа на следующее утро — **50–60%** (у лидеров скана), медианный гэп около нуля или слегка положительный. Гэп — это бонус; основной заработок — движение к целям +2/+5% в течение 24–48ч. **Не рекомендуется выходить по открытию** — после распродажных дней гэп часто негативный, а отскок идёт внутри дня.

## Ограничения

- Вероятности ухудшаются при смене рыночного режима (кризис, распродажа AI-сектора).
- Почти каждая вторая сделка не достигает цели +2% — управление риском обязательно.
- Данные: Nasdaq.com screener, Yahoo Finance.

## Вселенная (юниверс) — как она живёт

- **Состав:** все акции биржи NASDAQ с ценой ≥ $3 и капитализацией ≥ $500 млн (сейчас ~1500). Список берётся с официального скринера Nasdaq.com **при каждом прогоне** (`src/update_universe.py`).
- **Она меняется, но не уменьшается:** новые IPO добавляются, делистинги/банкротства удаляются, акции переходят границы цены/капитализации. Обычно 1400–1600 имён. Никакого «схлопывания» нет — обновляется автоматически.
- Из вселенной для скана отбирается **топ-60 по ликвидности** (оборот ≥ $5 млн/день), поэтому сканер всегда работает с торгуемыми акциями.
- Для новых имён история докачивается автоматически (полная 2-летняя), для старых — только инкрементально (быстро).

## Ручной запуск

Три способа:
1. **Кнопка на дашборде** («▶ Запустить скан сейчас») — если настроен секрет `SCAN_TRIGGER_TOKEN` (см. ниже). Триггерит workflow через `repository_dispatch`.
2. **Вкладка Actions** — открой `Actions → EOD Scanner (15:30 ET) → Run workflow`.
3. **Автоматически** — будни в 15:30 ET.

### Настройка кнопки на дашборде (одноразово)
1. Создай **fine-grained PAT** (`github.com → Settings → Developer settings → Personal access tokens → Fine-grained tokens`): доступ только к репозиторию `nasdaq-eod-momentum-scanner`, permission **Actions: Read and write**.
2. Добавь его как секрет репозитория: `Settings → Secrets and variables → Actions → New repository secret`, имя `SCAN_TRIGGER_TOKEN`.
3. Запусти workflow (кнопка в Actions) — дашборд пересоберётся с активной кнопкой.
   ⚠️ Этот токен встраивается в HTML-дашборд и виден в исходниках — поэтому давай ему **только Actions** (запуск сканов), не Contents и не другие права. Если не нужна кнопка — просто не настраивай секрет, кнопка будет вести на Actions.

## GitHub Pages — постоянный адрес дашборда

Дашборд публикуется на Pages каждым запуском. **Одноразовая настройка:**
`Settings → Pages → Source: GitHub Actions → Save`.

Постоянный адрес: `https://exodus611.github.io/nasdaq-eod-momentum-scanner/`

⚠️ Pages для приватного репозитория виден по ссылке любому (без авторизации). Не клади в дашборд ничего, что не готов показать публично. Код и данные остаются приватными в репозитории.
